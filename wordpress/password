MAMP
mysql --host=localhost -u root -proot

show databases;

use wp;

show table status

show tables;

SELECT DATABASE(); // permit de void la base courante utilize
 
select * from wp.wp_users; // void les champs

// change the password
update wp.wp_users SET user_pass=MD5('xav’);


Sauvegarde la DB dans un fichier


mysqldump -u root -proot xav > dumpfilename.sql

backup a table

# mysqldump -u root -ptroot xav contacts \
      > contacts.sql
